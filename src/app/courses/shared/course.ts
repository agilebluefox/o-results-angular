import { Control } from '../../controls/shared/control';

export class Course {
    location: string;
    name: string;
    mapdate: string;
    type: string;
    inorder: boolean;
    controls: Control[];
}